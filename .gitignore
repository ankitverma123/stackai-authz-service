# ── Internal working material — stays local, never shipped ──────────────
interviewAssignment.md
jobdescription.md
# Only the superpowers subtree is internal. docs/diagrams/ is a DELIVERABLE
# (spec §15) — ignoring docs/ wholesale would silently drop it from the repo.
docs/superpowers/

# ── Agent tooling — unrelated to the deliverable ────────────────────────
.claude/
.agents/
CLAUDE.md
skills-lock.json

# ── Secrets — never commit ──────────────────────────────────────────────
supabase_credentials.txt
*credentials*
.env
.env.*
!.env.example
*.pem
*.key

# ── Python ──────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.v/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ── Supabase ────────────────────────────────────────────────────────────
supabase/.branches/
supabase/.temp/

# ── Editors / OS ────────────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
